using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ObjectPoolManager : MonoBehaviour {
    
	// Use this for initialization
	void Start () {
        //ObjectPool objp = new ObjectPool();
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
